name: test schedule

on:
  schedule:
    - cron: '*/5 * * * *' 

jobs:
  build:
    runs-on: ubuntu-latest  
    steps:
      - uses: actions/checkout@v4
      - name: Test job
        run: echo "test"

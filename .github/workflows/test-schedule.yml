name: test schedule

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '*/5 * * * *' 

jobs:
  testjob:
    runs-on: ubuntu-latest  
    steps:
      - name: Test job
        run: echo "test"
